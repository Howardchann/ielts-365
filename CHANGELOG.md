# 变更日志（CHANGELOG）

> 本文件随代码留存，目的：任何接手的人（或下一次的 AI）都能看懂**改了什么、为什么改、遗留问题在哪**，不依赖对话上下文。
>
> 给 AI 看的决策背景见 [AI-CONTEXT.md](./AI-CONTEXT.md)。
>
> **阅读顺序：本文件按时间倒序，最新的在最上面。**
> ⚠️ 09-20 之前几节里的「云同步」相关描述**已作废**——云同步于 2026-09-21 整体移除（见下），保留原文仅为留存决策过程。
> ⚠️ 但 **2026-09-23 云同步已按用户决策恢复**（见最新一节），只是实现方式改为「云函数 + 服务端合并」，不再是当年的客户端直连数据库。

---

## 2026-09-24 · 测试台入仓纳管 + 真机实测清单

### 一、测试台迁入仓库

`D:/idea/_cloudsync_test/run_tests.js` → **`tools/cloudsync-test/run_tests.js`**。

动机：这两条最难的用例（【13】在途竞态、【14】服务端虚假新版本）原先只存在于仓库**外**的一个目录里，
换机器 / 重装 / 清理磁盘就没了，而且完全不受版本管理。现在纳入 git，跟代码一起走。

改动：

- 仓库根由硬编码 `D:/idea/ielts-365` 改为 `path.resolve(__dirname, '..', '..')` → **本目录可整体搬动**
- `TEST_STORE` 支持绝对路径（变异测试用）
- 迁入后重跑：**56 项通过 / 0 失败**，与迁移前一致

### 二、新增真机实测清单（`tools/cloudsync-test/真机实测清单.md`）

把 09-23 那次真机验收的经验固化成**可照做**的清单：三条前置、入口选择对照、四组用例（A 同步 / B 音频 / C 备份 / D 边界）、
云端核对命令、一张「一眼分辨这是不是 bug」的常见误判表。

**三条前置是最容易翻车的**（漏一条就会把正常现象当 bug 查半天）：

1. **「上传」只产生一个新的开发版，不会自动更新体验版** —— 走体验版必须在 mp 后台「版本管理 → 开发版本 → 选为体验版」
2. **两端必须是同一个微信号** —— `openid = f(appid, 微信号)`，不同号 = 两份记录，「同步不通」是正常的
3. **先导出备份** —— 真机实测会真的改动云端那条 `progress`（那是用户的真实进度）

### 三、验证状态

| 项 | 状态 |
|---|---|
| 测试台迁移后重跑 | ✅ 已验证：56 项通过 / 0 失败（退出码 0） |
| 仓库内引用路径同步更新 | ✅ 本文件两处 |
| 旧目录清理 | ✅ 已移除 `D:/idea/_cloudsync_test/` |
| README 与真机清单入仓 | ✅ `tools/cloudsync-test/` |

---

## 2026-09-24 · 修复同步在途（in-flight）竞态（`cloud-sync` 分支）

> ⚠️ **本节三个提交不是在本地 `git push` 上去的** —— 由用户在另一渠道（GitHub 网页 / API）直接提交到 `cloud-sync` 分支，作者邮箱是 GitHub noreply，本地 push 用的是 `stallonh@126.com`。
> 事后已本地 `pull` 对齐（fast-forward `13ce1ef` → `e89a9627`），并补写本节。
> 🔴 **云函数 `cloudfunctions/sync` 有代码改动 → 必须重新「上传并部署」。只重传小程序客户端无效，两边是配套的。**

### 一、问题

`store.js` 的 `applyMerged()` 在拿到云端回包后，会把本次上送单词的 `reviewDirty` 全部清空。但一次同步往返有几百毫秒，用户完全可能在这期间又复习了同一个词：

1. **新复习结果被吞掉**：回包清 dirty 时把飞行期间产生的新版本一起清掉 → 这次操作永远不会推上云，下一轮同步也不会再上送它。
2. **服务端制造「虚假新版本」**：云端对「与已有值完全相同的上送」也照样递增 `reviewRev` → 别的设备看到 rev 变了 → 误判 `needFull` → 每次同步都把全量复习记录拉一遍。

### 二、改动

| 提交 | 文件 | 改动 |
|---|---|---|
| `1e814f66` | `miniprogram/utils/store.js` | +13/−4 引入 `sentVersions`（上送那一刻的 `lastReviewedAt` 快照）；`applyMerged` 只清「发出时那一版」的 dirty，飞行期间已变化的词**保留 dirty**，交给下一轮续推 |
| `4120d9f9` | `miniprogram/utils/store.js` | +15/−1 修掉上一条的笔误（`sentVersions:` 悬空键 = 对象字面量语法错误）；新增 `inFlightLocal` —— 云端回包时把飞行期间的新版本**恢复回本地**并保留 dirty，不被回包覆盖 |
| `e89a9627` | `cloudfunctions/sync/index.js` | +14 新增 `sameReviewEntry()`；只有服务端最终值**真的发生变化**才进 patch / 写库 / `reviewRev + 1`，重复上传同一快照不再制造虚假新版本 |

### 三、验证状态

| 项 | 状态 |
|---|---|
| 本地与远端对齐（`13ce1ef` → `e89a9627`） | ✅ 已验证 |
| 测试台断言（`tools/cloudsync-test/run_tests.js`） | ✅ 已验证：**56 项通过 / 0 失败**（09-23 为 43 项；09-24 新增【13】在途竞态、【14】服务端虚假新版本，共 +13 项） |
| **新竞态路径本身**（请求飞行期间又复习同一个词） | ✅ **已验证（2026-09-24 新增用例【13】）** —— 在 `wx.cloud.callFunction` 处注入飞行期间的第二次复习，断言：回包不回退本地值、dirty 保留、云端最终 `correct`=2（两次都记上） |
| 用例本身的可靠性 | ✅ 已验证（**变异测试**）：把 `applyMerged` 的修复块换回旧逻辑（无条件清 dirty）后重跑，【13】精准失败 4 项（本地被顶回旧值、`correct` 停在 1、dirty 被误清、云端 `correct`=1）—— 证明用例真能抓这个 bug，不是空测 |
| 云函数重新部署 | ✅ **已验证（2026-09-24 15:41 独立复核）** —— `tcb fn code download` 下载云端代码与本地**逐字节一致**（16066 B，diff 0 处）；函数修改时间 2026-09-24 14:15:39；详见下方「部署核实证据」 |
| 客户端重新上传 | 🟡 **用户报告已完成（2026-09-24 14:16）** —— 命令行无法独立复核（须看开发者工具或后台版本信息） |
| 真机实测 | ❌ **未做** —— 按 `tools/cloudsync-test/真机实测清单.md` 执行（重点：A5 复习记录跨端） |

> 测试台支持 `TEST_STORE=<路径>` 环境变量覆盖被测的 `store.js`，用来做上述变异测试：把变异版放到 `miniprogram/utils/store.js` 同目录（它 `require('./data.js')`，不能挪走），跑完删掉即可。
> 用例【13】怎么构造竞态：`wx.cloud.callFunction` 是「payload 已固化、回包未处理」之间唯一的接缝 —— 包一层，在里面 `setTimeout(..., 5)` 后再注入第二次复习（间隔 ≥1ms 才能保证 `lastReviewedAt` 严格变大，否则 `currentAt > sentAt` 不成立、根本走不到被测分支），注入完才放行回包。

**部署核实证据（2026-09-24 15:41）**

| 核实项 | 手段 | 结果 |
|---|---|---|
| 云端代码是否等于本地 | `tcb fn code download sync` → `diff` | ✅ 16066 B / CRLF 389 个 / diff **0 处差异** |
| 本次修复特征串 | 云端代码里搜 `sameReviewEntry` | ✅ 命中第 109、324 行（与本地行号一致） |
| 函数状态 | `tcb fn detail sync` | ✅ `Deployment completed`，修改时间 **2026-09-24 14:15:39** |
| 云端配置 | 同上 | ✅ `Nodejs16.13` / 256MB / timeout 20 / `index.main` / 环境变量 None / 触发器 None —— 与 `cloudbaserc.json` 一致，**部署未弄丢任何配置** |
| 依赖是否装好 | `tcb fn invoke sync --params '{"action":"merge"}'` | ✅ 返回身份守卫拒绝（预期行为）；冷启动 585ms、执行 4ms，**证明 `wx-server-sdk` 正常加载**（否则会 `Cannot find module`） |
| 函数是否真的跑起来 | 同上 | ✅ `call succeeded`，入口在第 262 行守卫处返回，**未触碰数据库** |

> 部署过程踩坑：首次尝试报 `ResourceInUse.FunctionName`（*指定的FunctionName已存在，请勿重复创建*）—— 走成了「**新建**云函数」通道。`sync` 早已存在于云端，只能更新不能新建：右键必须落在 `cloudfunctions/sync` **文件夹本身** →「上传并部署：**云端安装依赖**」（或 `tcb fn deploy sync -e <环境ID> --force --yes`，`fn deploy` 是覆盖语义、不会报重名）。另注：项目内**没有** `cloudfunction-config.json`，函数也不读环境变量、无触发器 → 用开发者工具上传只更新代码，不动云端配置。

---

## 2026-09-23 · 新增「云同步版」分支（多端在线同步）

> **本仓库自此有两条分支**：`main` = 体验版（纯本地进度 + 导入导出备份）；`cloud-sync` = 云同步版（本节内容）。
> 两者共用课程数据与音频，差别只在 `utils/store.js`、`app.js`、设置页，外加一个云函数。**不要互相乱合。**

### 一、为什么把 09-21 删掉的云同步加回来

09-21 移除云同步的唯一理由是**付费**：官方规定免费体验环境在小程序发布后第 15 天转付费（约 19.9 元/月），当时的判断是"为多端同步付年费不划算"。
**用户拍板恢复**（个人版套餐，约 20 元/月；09-24 更正：**尚未实际付费**，环境处于个人版免费试用期至 2027-03-22，上线前需续费），门槛不存在了，且明确要"直接发布、能在线多端同步"的版本 → **决策反转**。旧文档里"别再引入云同步"的说法**已作废**。

### 二、实现（没有照抄历史代码）

历史实现（提交 `0102aa1` 之前）是**客户端直连数据库**：`wx.cloud.database().collection('progress').limit(1).get()`，一条记录存全部进度、整条覆盖写。这一版改为：

| 方面 | 旧实现 | 这一版 |
|---|---|---|
| 读写通道 | 客户端直连 `wx.cloud.database()` | **云函数 `sync`**（管理端身份），客户端完全不碰数据库 |
| 身份 | 靠数据库安全规则里的 `_openid` | `cloud.getWXContext().OPENID`，**无账号体系、无登录界面、无需改数据库权限** |
| 冲突处理 | 整条记录 last-write-wins（**另一台设备的进度会被整体覆盖掉**） | **先合并再写回**：逐字段按时间戳取更晚者 |
| 复习记录 | 与核心进度放同一条记录 | **按单词哈希分 8 片**（见坑 37） |
| 客户端合并 | 拉取结果直接覆盖本地 | 拉取结果**再与本地合并一次**，避免吞掉同步期间的新操作 |
| 备份格式 | v1 | v3（新增 `uncheckedDays` / `unstarredWords` / `settingsAt`，仍可读 v1/v2） |

### 三、改动清单

| 文件 | 改动 |
|---|---|
| `cloudfunctions/sync/index.js` | **新增**。`action:'merge'`：读云端全部记录 → 合并 → dotted-path 原子写回 → 返回合并真值 |
| `cloudfunctions/sync/package.json` | **新增**（`wx-server-sdk ~2.6.3`） |
| `cloudbaserc.json` | 注册 `sync`（timeout 20s） |
| `miniprogram/utils/store.js` | 打卡 / 收藏改存**时间戳**（旧值 `1` 仍兼容）；新增取消墓碑、`settingsAt`、`reviewRev` / `reviewDirty`；新增 `sync()` / `syncNow()` / `setSyncEnabled()` / `cloudStatus()` / `flush()`；备份升 v3 |
| `miniprogram/app.js` | `wx.cloud.init()` **提到 `store.init()` 之前**（见坑 38）；新增 `onShow` → `onResume`、`onHide` → `flush` |
| `miniprogram/pages/settings/*` | 新增「进度同步（多端）」卡片：开关 / 状态 / 上次同步时间 / 立即同步；「学习进度」与「关于」文案改掉"只保存在本机、不上传"（**隐私表述必须与实际行为一致**） |
| `README.md` / `AI-CONTEXT.md` | 分支说明、云端结构、合并规则、三个新坑 |

### 四、验证（端到端，不是只跑语法检查）

搭了 `D:/idea/_cloudsync_test/run_tests.js`（**2026-09-24 已迁入仓库 `tools/cloudsync-test/`，见该日一节**）：内存假数据库 + 把 `wx-server-sdk` 重定向到假实现，把**真实的 `store.js` 与真实的云函数**接起来跑，用两个 `store` 实例模拟两台设备 → **43 项断言全部通过**。覆盖：

- 集合不存在时自动创建并重试
- 两台设备各自打卡互不覆盖；**设备2 取消打卡后，设备1 带着旧数据再同步不会把它复活**
- 收藏 / 取消收藏同一套规则
- 复习记录取更晚一条；换设备能全量拿到（`reviewRev` 落后 → 返回 `reviewFull`）
- 设置项按 `settingsAt` 取更晚者
- 30 个单词全部落到正确分片；8000 词全量估算 804KB → 分 8 片后每片约 100KB
- 单次超 400 条分多轮推完，500 条全部到达云端
- 导入备份后能推给云端、且不会被云端旧值顶回去

**测试抓到 2 个真 bug（均已修）**：

1. **导入的收藏词会被静默丢弃** —— 备份里收藏词没有 `ts` 字段 → `toTs(undefined) = 0` → 合并逻辑把它当成"不存在"。修法：无 `ts` 时回落到备份的 `exportedAt`；服务端同步侧加 `|| 1` 兜底。
2. **积压补推太慢** —— 超过单次上限的复习记录要等 2.5 秒防抖才推下一批。修法：新增 `DRAIN_DELAY = 300ms` 快速通道。

### 五、已部署 / 已建资源（均已核验）

- 云函数 `sync` 已部署（`tcb fn deploy sync`，2026-09-23 14:32，状态 `Deployment completed`）
- 云数据库集合 `progress` 已创建（`tcb db nosql execute` 执行 `{"create":"progress"}`）；真机验收后已有真实记录（1 条核心文档 `_id = <openid>`，复习记录分片按需生成）
- 真实环境调用一次 → 返回 `{"ok":false,"error":"无法识别用户身份（OPENID 为空）"}`：说明 CLI / 外部调用**拿不到 openid 就读写不了任何人的进度**，身份守卫有效（这条同时是安全属性，别删）

### 六、真机验收（2026-09-23 完成，云端数据独立核对）

用户在**真机（手机）+ 开发者工具**两端实测，云端 `progress` 文档快照逐项印证：

| 验收项 | 结果 | 云端证据 |
|---|---|---|
| 首启自动建云端记录 | 通过 | 存在 `_id = <openid>` 的核心文档 |
| 打卡推云（约 2.5s 防抖） | 通过 | `checkedDays.w1d1` 有值 |
| 收藏跨端同步 | 通过 | `starredWords` 含词条（`w/m/p/e/ts` 齐全） |
| **取消打卡 / 取消收藏后另一端不复活**（墓碑） | 通过 | 见下 |
| **墓碑非永久封锁——取消后仍能重新收藏** | 通过 | 重新收藏 ts 比旧墓碑晚 **55.0 秒**，`starredWords` 胜出 |
| 设置页关掉同步后不再推送 | 通过（间接） | 冻结期内云端只收到另一端的写入，解冻后旧数据才一次性推上来（`updatedAt` 序列与操作时序一致） |

**墓碑为什么必须专门构造才能测到**：不能只"两端各点一次同步"——`buildPayload()` 每次推的是**全量 core**，
两端推相同的最新状态时裁决器永远不会出错，**测不出墓碑有没有生效**。必须构造**一端持有旧数据**的局面：
关掉该端同步开关使其冻结 → 另一端取消 → 重新打开开关（`setSyncEnabled(true)` 内部直接 `syncNow()`，
于是带着旧数据整份推上来）。实测时序：取消收藏 16:49:42 → 持旧数据端在 **16:51:52**（晚 129.6 秒）
又完整推过一次全量 core → 云端 `starredWords` 仍为空、`unstarredWords.name` 仍在 → **旧数据未能复活取消**。
打卡组同理（取消打卡比打卡晚 35.3 秒，胜出者为"取消"）。

**⚠️ 一个极易误判成 bug 的良性现象**：云端文档里 `checkedDays.w1d1` 与 `uncheckedDays.w1d1`
**同时存在**（某词已从 `starredWords` 移除、`unstarredWords` 仍留其墓碑也一样）。
原因：**云开发 `update({data})` 对对象字段是合并语义（不是整体替换）**，只 `set` 不 `delete`，旧键必然残留；
**正确性由读取侧的"取 ts 更晚者"保证**（`mergeDays` / `mergeStars` / `mergeFlagMaps` / `mergeStarMaps`），
返回给客户端的 `core` 只含胜出者。
→ **不要试图用"传一个完整的 map"来删键，删不掉**；真要清理得用 `db.command.remove()` 显式删字段。

- ⚠️ 发布仍受**备案**约束（通管局审核中），备案通过前只能停在体验版

---

## 2026-09-23 · 进度改打卡驱动 + 例句播放按钮 + 句首拖音修复（61 句换 Emma 音色）

### 一、进度跳日（用户反馈 #1）——**行为变更**

**原逻辑**：`today.js onShow()` 默认落点 = `currentDayFromStart()`（从开始日起的自然日天数），与是否学习无关 → 没学 / 没学完，第二天也跳到新内容。

**新逻辑**：默认落点 = `store.firstUnfinishedDay()`（**打卡驱动**）——第一个"非周六弹性日且未打卡"的学习日。没学完，第二天打开仍是这一天；打卡后推进到下一未完成天。全部完成回退自然日。周表跳转（jump）优先级最高，「回到今天 Day N」仍以自然日为参照。

- `store.js` 新增 `firstUnfinishedDay()`（跳过 `type:'sat'` 与无内容天，遍历 546 天）
- 影响文件：`utils/store.js`、`pages/today/today.js`（各 ~10 行）

### 二、单词例句加播放按钮（用户反馈 #3）

用户的朋友不知道"点句子文字能读" → 单词卡的例句行改为 `.word-example-row`（文本 `flex:1;min-width:0` + 右侧 `.ex-speak` 🔊 按钮），与语法区同款（含 `playing` 反色、`speak-hover` 按下反馈）。点击行为与原点文本一致（`data-ai`/`data-kind="s"` 保留，仍走编号预生成）。
- 影响文件：`pages/today/today.wxml`、`app.wxss`

### 三、句首断裂排查（用户反馈 #2）——**实锤句定位到 TTS 韵律模型：Ava 会把无标点句自行切成语调短语并拖长首词**

用户反馈"还是有句子第一个单词单独读、与后面不连续"。完整排查链：

| 嫌疑 | 验证方法 | 结果 |
|---|---|---|
| 生成端文本拼接 | 读 `generate_v2.py` / `gen_grammar.py` | 都是整句直出，无 "词+句" 拼接 |
| 语法句哈希 miss → 降级在线 | 运行时 765 句 vs 预生成清单哈希核对 | **765/765 全命中** |
| 复用旧料音色/引擎不同 | ① 正慢速时长比：复用组 1.405 vs 新生成组 1.408（-30% 理论 1.43）② 句首停顿比例：7.7% vs 10.2%（中位 0.43s） | **两组一致，旧料排除** |
| 音频内异常断裂（第一轮，⚠️ 统计已作废） | `ffmpeg silencedetect` 扫语法区 2268 个音频，对照文本标点 | 初判"停顿全部能被标点解释"→ **该结论随后被实锤推翻**；且本轮比例统计受**解析 bug** 污染（把句尾静音当句中停顿，见下"追加修正"） |

**实锤与最终结论（当日稍晚）**：用户给出具体句子 —— **Day 2 单词 other 的例句 "He and the others are with her."（idx 18）**。"He" 后**无任何标点**，实测：

- 正常速 2.50s，**0.53s→0.95s 存在 0.425s 静音**；慢速同样有；
- 用当日最新 edge-tts、同参数（`en-US-AvaMultilingualNeural` / `+0%` / `-30%`）**重新合成 → 停顿毫秒级复现**（0.425s，位置一致）。

→ 定论：这是 **AvaMultilingual 韵律模型自行插入的换气停顿**，确定性行为，重生成、改文本、重传都无效，**不是管线 bug**。"停顿全由标点解释"的初判作废。

**修复尝试与决策**：
- `silenceremove` 不可用（ffmpeg 8.1 实测：`stop_duration` 是触发阈值且与 `stop_silence` 组合时完全不删）→ 改用两段式修法 `_fix_pause_one.py`（`silencedetect` 取时间戳 + `atrim/concat` 重组），长停顿截为可配值（0.05/0.12/0.20/0.25 四档已生成对比样本）。
- ⚠️ **此路线随后被用户否掉**（"不管长短都奇怪，等于重音拖在了 He 上"）→ 见本节末尾"再追加"，真正根因是**音节被拖长**，不是静音。这部分保留仅为记录探索过程。

**追加修正（同日）—— 解析 bug 与全量重扫**：用户追问"是否每处停顿都得人耳判断"，促成本轮彻底复核，发现前四次统计共用一个解析 bug：**把 silencedetect 输出的 `silence_duration` 当成 `silence_end` 使用**，导致句尾留白被误判为句中停顿，得出"80.8% 例句有内部停顿"等荒谬数字。重写 `scan_pause_v2.py`（12 进程并行 + 正确三元组解析 + 丢弃未闭合的半截静音段），18268 个文件 **10 分钟**扫完，实测：

| 范围 | 有句中停顿 ≥0.3s 的文件 | 占比 |
|---|---|---|
| s/（例句，16000 文件含双速） | 620 | 3.9% |
| g/（语法句，2268 文件含双速） | 729 | 32.1% |
| 合计 | **1349** | **7.4%** |

正常速 586 条按文本分类：**句中完全无标点 = 61 条（A 类，机器可 100% 判定为模型换气，其中 ≥0.4s 的 33 条）**；句中有标点、需词级对齐才能判定 = 392 条（B 类）；未映射 133 条（旧件/命名不匹配）。

→ **结论：自动化可行，不需要逐条人耳判断**——A 类由"文本无标点 + 存在停顿"零判断筛出；B 类用 edge-tts 词级时间戳对齐即可自动分流。人耳只需一次用于**校准截短档位**。

**再追加（同日）—— 用户否掉"截短"路线，根因定位到音色**：用户听完 4 个截短档位（0.20/0.12/0.05/0.00s）反馈"**不管长短都奇怪，等于重音拖在了 He 上**"。改用 edge-tts **词级时间戳**（`Communicate(..., boundary='WordBoundary')`，默认 `SentenceBoundary` 拿不到）诊断，真相是：

- 目标句 "He" 实际时长 **0.46s**（对照句 "He is my brother." 里仅 0.15s）→ **音节本身被拉长 3 倍**，后面再接 0.37s 空档；
- 换 `She and the others are with her.` 同样拉长到 0.46s → **与具体单词无关，是 AvaMultilingual 把无标点句子自行切成多个语调短语**；
- 因此**截静音治不了本病**（方向错了），小写 `he`（0.40s）、加逗号 `He,`（0.34s）也都没恢复正常。

**跨音色实测（61 条 A 类文本 + 200 句随机语料）**：

| 音色 / 参数 | A 类 61 条中出现空档 | 200 句语料中出现空档 | 其中"无标点处" |
|---|---|---|---|
| AvaMultilingual（现用） | 36 | 12（6.0%） | 2 |
| Ava + rate +5% / +10% / pitch -3Hz | 32 / 31 / 33 | — | — |
| **AvaNeural（非多语种）** | **0** | 8（4.0%） | **0** |
| **EmmaMultilingual** | **0** | 7（3.5%） | **0** |

→ **语料整体健康**（异常断句 <1%），问题集中在 Ava 的无标点断句。**同音色微调无效，只有换音色能根治**；全量重生成不划算（受影响 <1%）。

**当时给出的两条路**（样本：`_wordbank/_voice_s1_{ava,avan,emma}.mp3`、`_alt_fix_18_t145.mp3` / `_alt_fix_18_t170.mp3`）：
1. **换音色重生成这 61 句**——音色一致性有损，但量小（<1%）；
2. **保留 Ava 做后期处理**——压缩拖长音节 + 截空档，音色统一但属 hack。

→ **用户决策（13:46）：走第 1 条** — "这些音色中还是就 AvaMultilingual 最自然，然后把那几条拖音的音色换成 Emma 的吧"。执行记录见下节「四」。

**临时验证脚本**（可复用）：`_wordbank/detect_pause.py`、`detect_g_detail.py`、`detect_s_reuse.py`（三者均含上述解析 bug，**勿再直接使用**）、`_fix_pause_one.py`（截静音，**对本病无效**）、`_fix_stretch_one.py`（压缩拖音+截空档）、`scan_pause_v2.py`（全量清单，**正确版**；结果 `internal_pause_v2.json`）、`_probe_boundary.py` / `_probe_voices.py` / `_probe_Aclass.py` / `_probe_variants.py` / `_probe_corpus_voices.py`（音色与词级诊断）、`_make_voice_samples.py`（试听样本）。

### 四、句首拖音修复：61 句换 Emma 音色（当日执行完毕）

**决策**：主音色继续用 AvaMultilingual（用户听感最自然），仅把有拖音的句子换成 EmmaMultilingual。

**替换范围 = 61 句 / 122 个文件**（单词例句 56 + 语法句 5）。
**筛选口径**：现有分发文件实测存在 ≥0.30s 句中停顿（`silence_start>0.3 且 silence_end<总长-0.35 且 dur≥0.30`）**且**该句文本句中无任何标点（有标点的停顿是正常韵律，不算问题）。

| 步骤 | 数据 | 状态 |
|---|---|---|
| 换前词级复核（`boundary='WordBoundary'`） | Ava **36/61** 有 ≥0.25s 词间空档 → **Emma 0/61**；Emma slow 仅 1 句 0.54s | 已验证 |
| Emma 双速重合成（`+0%` / `-30%`） | 122/122 成功 | 已验证 |
| 转码 44100Hz/mono/96kbps + `silencedetect` 复测 | 新文件 normal **0/61**、slow **0/61** 仍有异常（对照：原线上 normal **61/61** 全有） | 已验证 |
| 覆盖 dist（`dist2/s`、`dist_g/g`）+ raw（`raw2/s`、`raw_g`） | 244 个写入；md5 **122/122 + 122/122** 一致 | 已验证 |
| 云端上传（`tcb storage upload`，按分片目录共 12 次） | 全部 `√`，`Failed to upload 0 file(s)` | 已验证 |
| 云端全量对账（`storage list` 解析 ETag vs 本地 md5） | **122/122 一致**，0 不一致、0 未找到 | 已验证 |
| 原件备份 | `D:\idea\_wordbank\_swap_bak\`（244 个，可原地恢复） | 已验证 |

**本轮新踩的两个坑**

1. **`generate_v2.py` 的「复用旧料」会把非 Ava 音频混进线上**：该脚本对 `+0%` 条目先查旧 `raw/`（md5 命名）目录，命中就 `copyfile` 直接复用。本轮发现约 **17 句"实测有停顿、Ava 重合成却完全正常"**——说明这批线上文件并非 Ava 产物（历史复用的旧料）。替换时一并修正。
2. **替换音频必须同时覆盖 `raw`**（`raw2/s`、`raw_g`）。只覆盖 `dist` 的话，生成脚本的「断点续跑」（文件存在且 >512B 即跳过）会把旧音色留在 `raw`，日后一旦重新转码/重传就回退。**dist 与 raw 必须成对更新。**

**客户端生效说明（重要）**：`speech.js` 优先用本地缓存——`exists(lp)` 命中就直接播本地文件、不问云端。因此**已经播放并缓存过这些音频的设备仍会播旧版**；未播放过的会拉到新版。需要立即生效只能清小程序缓存/删除重进。

**待清理（附带发现）**：`dist_g/g` 下有 djb2 负数哈希残留文件（形如 `-1c1d0f5.mp3`），是第一批命名 bug 的遗留物，云函数白名单 `^g/[0-9]/[0-9a-f]{8}s?\.mp3$` 不认识。

> 🔧 **2026-09-23 已清理，并修正了此前记错的数字**：文档与备忘里原先写的是"313 个"（另有"369"一说，那是**重生成**的文件数，不是残留数），**实际只有 96 个 / 4.21MB**。
> 构成：`-1`~`-9` 九个纯垃圾目录共 **88 个** + `0` 目录里混着的 **8 个**。
> ⚠️ `0` 目录里同时有 **202 个正常文件**，所以**只删那 8 个文件、没有删目录**。
> 已移入 `D:\idea\_wordbank\_trash_neg_20260923\`（完全可逆）—— 本想走系统回收站，但本机 PowerShell 的 `Add-Type` 与 COM 实例化都被安全策略拦下，改用专用回收目录。
> 云端 `g/-1`~`g/-3` 抽查全空 → **这批文件当年从未上传成功**，清理只需动本地。清理后 `dist_g`：负数 0 / 正常 2172。

**本轮脚本**（均在 `_wordbank/`，`_` 前缀 = 临时件）：`_plan_swap.py`（筛选）、`_verify_swap.py`（词级复核）、`_swap_gen.py`（合成）、`_swap_verify.py`（转码+验收）、`_swap_apply.py`（备份+覆盖）、`_swap_upload.py`（上传）、`_swap_check_cloud.py`（云端对账）、`_swap_report.py`（分布+样本）。
- 清单：`_swap_list_report.txt`；实听对比：`_swap_listen/{base}_old_ava.mp3` / `{base}_new_emma.mp3`

---

## 2026-09-22 · 音频上云（云函数签名）+ 语法句预生成 + 三轮真机反馈闭环

### 一、事件线

| 时间 | 事件 |
|---|---|
| 上午 | 拿到环境 ID 与 fileID → 32000 条音频上传云端 → **发现免费期存储权限被锁死** → 改云函数签名方案 → 真机验证通过 |
| 中午 | 用户真机日志报 `backgroundfetch privacy fail` → 判定为误报（见下） |
| 13:50 | 用户提 **7 条修复**（语法句逐词蹦/音色不一、底栏重叠、关于文案位置、口音、音源切换、排版） |
| 15:00 | 语法句预生成根治（含 djb2 负数哈希 bug）→ 三轮真机反馈逐项闭环 |
| 16:20 | 云端垃圾清理 + GitHub 推送（3 个 commit） |

### 二、云端音频（提交 `087b41e`）

| # | 类型 | 文件 | 内容 |
|---|---|---|---|
| 1 | 新增 | `cloudfunctions/audio-url/index.js` | 批量签发云存储临时链接；`MAX_BATCH=50`、`MAX_AGE=7200` 写死、路径白名单正则 |
| 2 | 新增 | `cloudbaserc.json` + `project.config.json` | envId、functionRoot、cloudfunctionRoot |
| 3 | 改造 | `utils/cloud.js` | `ready()` 改为看 `wx.cloud.callFunction`；新增 `SIGN_FN='audio-url'` |
| 4 | 改造 | `utils/speech.js` | 云存储档由 `downloadFile` 改为「云函数批量签名 → URL 缓存(TTL 60min) → `InnerAudioContext` 播 https → 后台落本地缓存」；签名走**串行队列**防漏签；播放失败丢弃 URL 并降级在线链 |
| 5 | 新增 | `utils/prefetch()` | 提前签好当天音频（分片 ≤50/批），并顺手落盘前 10 条 |

**为什么必须这么做**：免费套餐下云存储权限锁定为「仅创建者和管理员可读写」，控制台与后台 API 双双拦截修改（`ModifyStorageSafeRule 当前套餐无法执行此操作`）；音频由管理端 CLI 上传、不属于任何用户，客户端 `downloadFile` / `getTempFileURL` 一律被安全规则拒绝（实测无签名直链 403，桶私有）。

**验证**：云函数返回 3 条签名 URL；非法路径 `../../secret.txt` 被拦；签名 URL 下载字节数与本地一致（39542 / 18853 / 16972 B）；mock 行为测试 `_test_speech_signed.js` **14/14 PASS**。
**真机验证（11:03）**：单词有声、例句有声、慢速 0.75X 有声、备份导出导入正常 → 音频链路闭环；并证明 **`InnerAudioContext` 播 https 直链不受 downloadFile 域名白名单限制**。

### 三、语法句预生成（本轮最大修复）

**根因**：语法例句在数据里是纯字符串数组 `day.g.ex[]`，**没有音频编号**，客户端只能降级在线 TTS → 音色不统一、部分仍逐词蹦。

**修复**：为全部英文语法例句预生成音频，命名用**文本 djb2 哈希** → `g/{hash%10}/{hash}{s?}.mp3`，数据文件零改动。

| # | 类型 | 内容 | 状态 |
|---|---|---|---|
| 6 | 修复 | 统计：例句引用 912 条去重 = **765 英文（预生成）+ 147 含中文（记忆法条目，走在线）** | 已验证 |
| 7 | **修复 bug** | **djb2 必须 `>>> 0`**：初版 `& 0xFFFFFFFF` 得 int32，>2^31 的句子哈希为负 → 文件名带负号、分桶 `-1`~`-9`、白名单匹配不上（88 文件传不上）。修复后重提取，369 个文件重生成 | 已验证 |
| 8 | 修复 | `cloudfunctions/audio-url` 白名单扩为 `^([ws]/[0-8]/\d{4}s?\.mp3\|g/[0-9]/[0-9a-f]{8}s?\.mp3)$` 并重新部署 | 已验证 |
| 9 | 修复 | `speech.js tryPregenG()` 加非 ASCII 短路：中文条目不再白跑一次云函数 + 播放报错 + 6 秒超时 | 已验证 |
| 10 | 验证 | 生成+转码 **1530/1530 零失败**；全量差集核对（`verify_grammar_all.py`）：本地缺失 0 / 云端缺失 0 | 已验证 |

### 四、其余 6 项修复（提交 `ba72e28` 及前置）

| # | 类型 | 文件 | 内容 |
|---|---|---|---|
| 11 | 修复 | `app.wxss` | 底部模块与 tabBar 重叠 → `page` 加 `padding-bottom: calc(40rpx + env(safe-area-inset-bottom))` |
| 12 | 修复 | `utils/speech.js` + `settings.js` | 新增 `engineMode`（auto / pregen / online）+ 设置页三选。**逐词连播只保留给单词**（句子兜底改为 有道整句 → 百度整句） |
| 13 | **修复 bug** | `utils/speech.js tryUrl()` | 重构中把「precheck 被拦 → 退回直连」的兜底丢了 → 域名未配时整句直接放弃不出声。**这条是行为测试抓出来的**，复跑 14/14 |
| 14 | 修复 | `settings.js` | 「关于」文案集中到顶部 5 个常量，用户可自行修改；wxml 去掉写死的 `v` 前缀（否则用户写成"版本V1.0"会显示重复 v） |
| 15 | 修复 | `today.wxml/js/wxss` | 练习任务 `pr` 按 `1) 2) 3)` 拆行渲染 |
| 16 | **修复 bug** | `app.wxss` | 语法区文字"全挤在一行"——根因是 CSS：`.grammar-ex-text{flex:1}` **缺 `min-width:0`**，长英文句被撑成一行。补 `min-width:0` + 折行属性 + 行高；新增 `.ex-no` 序号圆标、`.grammar-ex` 分隔线 |
| 17 | 修复 | `today.wxml` + `app.wxss` | 语法区播放按钮点击无状态：按钮本身没挂 `playing` 类、也没有 `.ex-speak.playing` 样式 → 补样式；按下反馈改用 `hover-class="speak-hover" hover-stay-time="120"`（小程序 `view` 的 CSS `:active` 不可靠） |

**真机复验**：三轮反馈全部闭环——语法句音色统一且连贯 ✓ / 按钮状态与按下反馈 ✓ / 音源切换 ✓ / 底部间距 ✓ / 任务区排版 ✓ / 语法区长句折行 + 序号圆标 ✓ / 「关于」卡片两行 ✓。

### 五、云端垃圾清理 + GitHub 推送（用户批准后执行）

- **清理**：`diff_g_cloud.py` 以「运行时真正会请求的 1530 个路径」与云端 `g/0~g/9` 全量做差集 → 缺失 0、**多余 650 个（27.11MB）**。构成为 **12 个负号哈希 + 638 个"合法 hex 但不在需求集内"**（第一批生成时的旧哈希命名，与原先"都是负号文件"的推测不符）。删前核对 `speech.js hashName` ↔ 核对脚本 ↔ 云函数 `PATH_RE` **三处逐字一致**，再执行 `clean_g_extra.py` → 复验云端 **1530 个 / 64.4MB，缺失 0、多余 0**
- **推送**：3 个 commit —— `966b2dd`（8000 词 + 78 周，16 文件）→ `087b41e`（音频云端分发，7 文件）→ `ba72e28`（语法区/设置页，4 文件）
- ⚠️ **网络层踩坑（重要）**：沙箱内 push 挂起，沙箱外直连也失败。根因是**本机 DNS 拒绝解析 github.com**（`ECONNREFUSED queryA`），11 个 GitHub IP 中仅 4 个可达 → 「DNS 挂 + GitHub 半阻断」，**不是代理的问题**。解法：本地 CONNECT 隧道 + **`git -c http.sslBackend=openssl -c http.proxy=http://127.0.0.1:18899`**（默认 schannel 后端走隧道完全失败；隧道脚本须 `client.pause()` 后再 pipe，否则丢 TLS 握手数据）
- 三方对齐校验：`refs/heads/main` == `origin/main` == `ls-remote` == `ba72e289…`；另用 API 独立复核三个 commit 均已上线

### 六、误报排查：`backgroundfetch privacy fail`

真机日志 `private_getBackgroundFetchData:fail:internal error` 属**噪声，与业务无关**。两条硬证据：① 全项目 grep 无 `getBackgroundFetchData` / `backgroundFetch` / `usePrivacyCheck` 调用；② 朗读链路用到的 `wx.cloud.callFunction` / `wx.createInnerAudioContext` / `wx.downloadFile` **均不在隐私接口清单**。

⚠️ 但排查中发现一项**提审前必办**：本项目唯一的隐私接口是 **`wx.setClipboardData`**（导出备份）——官方把 set/get 同列归入"读取你的剪切板"，**写入也算**。提交审核时必须在「用户隐私保护指引」声明剪切板，否则**正式版**该功能 fail。

---

### 七、发布路径确认：走体验版，不发布正式版（17:35，仅文档改动）

**用户决策**：优化点已清空 → 走**体验版**，不提交审核。核实后确认成立，且是当前最省事的路。

| 项 | 结论 | 依据 |
|---|---|---|
| 体验版要不要审核 | **不要**。工具「上传」→ 后台「版本管理 → 选为体验版」 | 官方 |
| 体验版会不会触发「发布上线」 | **不会** → 免费云环境不受影响（转付费的触发条件是**正式发布后第 15 天**） | 官方计费文档 |
| 有效期 | 无固定过期时间（「试用版」才是 14 天自动注销） | 通行实践 |
| 体验成员上限 | 个人主体 **15**；未认证未发布的非个人 30；已认证未发布 / 未认证已发布非个人 60；已认证已发布非个人 90 | 官方 |
| 访问权限 | **白名单制**：非成员扫同一个二维码提示「无权限访问」，官方明确「**不支持公开临时权限**」→ 必须先把对方**微信号**加进「成员管理 → 体验成员」；想"扫码即用"只能发布正式版 | 官方（开放社区） |
| 提审的前置条件 | **必须先完成小程序备案**（2023-09-01 起，未备案不能提交代码审核）。用户状态（2026-09-22）：**类目已通过、备案在通管局审核中**（1–20 个工作日、不可催）。**备案期间不影响体验版** | 官方 |
| 体验版拿不到的 | 微信内搜不到、**无法生成可长按识别的小程序码**、不支持微信支付 | 官方 + 实践 |
| ⚠️ 上体验版第一件事 | 体验版**校验服务器域名**，只有「真机调试」不校验 → **音频直链与云函数签名必须在体验版上复验** | 官方 |

**文档同步**：`AI-CONTEXT.md` §8 新增「体验版的实际能力」表；`README.md` §7 拆出「不用走审核」路径，类目与隐私声明改标为「提交审核才需要」。两份文档表格结构校验通过。

> 待盯：微信侧免费活动**明文截止 2026-12-31**（调整提前 3 个月通知）→ **2026-10 初留意开放社区 / 腾讯云公告**；本环境 Expiration 2027-03-22 23:59:59。

---

## 2026-09-21 · 移除云同步 → 换源预生成 → 8000 词 / 78 周重构

### 一、架构决策：进度改纯本地 + 内置 SRS（提交 `0102aa1`、`4e5c40d`、`51b5702`）

| # | 类型 | 内容 |
|---|---|---|
| 1 | **移除** | 删除云同步全部代码（`store.js`）、设置页云同步卡片、`app.js` 云开发初始化。**当前全项目无任何 `wx.cloud.database` / `.collection(` 调用** |
| 2 | 新增 | 本地间隔重复：`DEFAULT_INTERVALS = [1,2,4,7,14,30,60,120]`；答对 `level+1`，答错 `level=0` 且 `nextReviewAt=now` |
| 3 | 新增 | 复习页三页签：随机 / **到期** / 重点词（`dueWords()`） |
| 4 | 修复 | 备份合并幂等：`mergeReviewStats()` 按 `lastReviewedAt` 取较新者，同一份备份重复导入不会重复累加 |

**为什么移除**：免费云环境在小程序发布后第 15 天到期（转付费约 19.9 元/月），而本项目实际用量仅约 8 资源点/月（占免费额度 0.02%）——**门槛是政策倒计时，不是用量**。为「多端同步」付年费不划算，改为自带备份自救。

### 二、语音方案换源：逐词连播作废 → 云厂商 TTS 预生成

用户否决逐词连播（"逐词读句子对我这种零基础学习的来说本身就很难受"）——连读/重音/语调/句子节奏全丢，而这是零基础学句子最需要的。**该方案作废，只保留给单词。**

**端点全覆盖实测（决定换源）**：

| 端点 | 结果 |
|---|---|
| 有道 `dictvoice` | 参数无解（`type`/`le`/`keyfrom`/去标点全 500）；**与长度相关**：`I usually` ✅ / `I usually get` ❌ |
| `tts.youdao.com/fanyivoice`\|`nspeech`\|`ttsapi` | HTTP 688（WAF 封，补 UA/Referer 无解） |
| `tts.baidu.com/text2audio` | `err_no:503 Failed connect to jtts engine` |
| 必应 readaloud / Google translate_tts | 403 / 超时 |
| **覆盖率量化** | 单词 **99/100**，**例句仅 11/100** → "部分句子不发声"实际是**约 89% 例句都不发声** |

**音色定稿**：`en-US-AvaMultilingualNeural`（用户试听 6 个音色后选定"最接近人声"）。
**规格定稿**：转码 **44100Hz 单声道 MP3** —— 微软原生输出 MPEG2 24kHz，而百度那批"播放报错"的正是 MPEG2 低采样率档；44100Hz MPEG1 是全机型验证可播的规格。

**码率天花板实测**（内存内 patch `communicate.py` 的 `outputFormat` 逐档测）：仅 **24kHz 家族**可用，**码率上限 96kbps**；`audio-24khz-128/160/192kbitrate`、`audio-48khz-*`、`audio-16khz-128kbitrate` 全部 `NoAudioReceived`。

### 三、词表扩至 8000 + 课程重构为 78 周（提交 `966b2dd`）

| # | 类型 | 内容 |
|---|---|---|
| 5 | 新增 | `utils/words.js`（627.8KB）：8000 词，**课程顺序**，每行 `单词\t词性\t释义\t例句\t音频编号` |
| 6 | 改造 | `utils/data.js` 重写：`TOTAL_DAYS=546`、`WEEK_COUNT=78`、`RAMP[78]`、`CORE_CNT[390]`、`DAY_OFF` 偏移表；`getDay()` 装配词条 |
| 7 | 改造 | `data-p1..p4` → **`data-p1..p6`**（78 周教学内容，每片 13 周；词数引用改占位符 `{D}`/`{W}`/`{Wp}`/`{P1}`） |
| 8 | 改造 | 页面适配：`today`（核心词/扩展词分组）、`weeks`（18 个月总览）、`review`（传 ai/kind）、`settings`（关于文案、日期 picker 放宽到 2028-12-31） |

**排法与坡道**：

```
W1, W2 → R1 → W3, W4 → R2 → …… → W49, W50 → R25 → W51 → R26 → W52
78 周 / 546 天 / 390 个学习日；新课日 14→30 词/天（260 天）+ 巩固日 11→25（130 天）= 8000
```

- **必须新写 130 天内容**的原因：原课程教学内容恰好 52 周整（260 个语法点全部唯一 + 52 个周日自检），且**降词量必须增加"带新词的学习日"**——只加复习日词量不会降
- 26 个巩固周每周一个技能主线（复习方法 → 前缀 re-/un-/dis- → make/do/take 搭配 → …… → 词根 struct/tract/form → 考前衔接），**不引入新语法**；构词法周的例词从 8000 词表按词频真实筛选
- 采用**核心词 + 扩展词双轨**：1125 个原有主题词留在原周原位，6875 个新词按词频升序追加 → **周主题/每日标题/语法/练习/自检一行都不用改**
- ⚠️ **音频零重做**：`ai` = 词频序排名，与课程结构解耦，**32000 条音频一条都不用重生成、不用重新上传**
- W52 是「考前一周：状态巅峰」，必须留在最末 → 第 26 个巩固周刻意插在 W51 之后

### 四、主包体积硬约束（实测，差点爆限制）

| 编码方式 | 体积 |
|---|---|
| 原 JS 对象字面量风格外推 8000 词 | 2044KB → 主包 **107%，爆 2MB 限制** ❌ |
| **制表符/换行分隔字符串**（采用） | **628KB → 主包 55%** ✅ |
| 对象数组 JSON / 元组数组 JSON / 去音标 | 886KB / 722KB / 550KB |

另：**音频命名从"文本哈希"改为"词表序号"** —— 8000 词的哈希映射表会膨胀到 1.2MB+，直接吃掉 2MB 主包；序号命名让运行时靠 `ai` 拼路径，**零映射表**。`{ai//1000}/` 分片目录避免单目录上万文件（Windows 上删除 1.6 万文件 12 分钟未完成）。

### 五、本轮踩坑

- **同一文件并行发 Edit → 改动静默丢失**（`weeks.js` / `settings.js` 各丢 3 处）。教训：同文件修改必须串行，或整文件重写；改完必须 `grep` 复验（**09-22 又咬了一次**，见上节第 21 条）
- `data-p*.js` 字符串里换行必须转义成 `\n`，否则 `Unterminated string literal`（`node --check` 立即发现）
- utils 备份目录必须放在 `miniprogram/` **之外**，否则被计入主包
- 音标补齐：117 个派生词用 eng-to-ipa(CMUdict) 生成，**必须用最长匹配分词器**（连续 replace 会把 ɪ→i 再被 i→: 二次转换，产出 `si:g'ni:fi:kәntli:` 这种错音标）
- 例句抓取首轮过滤太松（`repressed` 配到不当例句）→ 学习产品绝不能有，大幅扩展敏感词表后重建
- 人名表误伤 `frank/mark/rose/may/jack/bob` 等**同时是常用词**的名字 → 从人名表移出

---

## 2026-09-20（第二次）· 进度备份 + 云状态显示修正

由于确认「免费云环境会在发布后第 15 天到期」，且个人主体无法迁移，决定**不依赖任何付费云服务**，改为自带备份自救。

| # | 类型 | 文件 | 内容 | 状态 |
|---|---|---|---|---|
| 1 | 修复 | `utils/store.js` | 新增 `exportBackup()`：把 startDate / checkedDays / starredWords / rate / accent / engine 序列化为带 `app` 标识与版本号的 JSON 文本 | 已验证 |
| 2 | 修复 | `utils/store.js` | 新增 `importBackup(text, mode)`：`merge` 取并集（打卡合并、重点词按 word 去重且保留本地释义）、`replace` 整体覆盖 | 已验证 |
| 3 | 修复 | `utils/store.js` | 新增 `sanitize()`：外部数据逐字段校验——key 须匹配 `/^w\d{1,2}d[1-7]$/`（防 `__proto__` 污染）、缺 `w` 的条目丢弃、字符串截断（词 60 / 释义 80 / 词性 20 / 例句 300）、`rate` 限 0.5~2、`accent` 限 us/uk、`engine` 限三者之一、日期须 `YYYY-MM-DD` | 已验证 |
| 4 | 修复 | `pages/settings/settings.js` | 新增导出（自动复制剪贴板）、重新复制、合并导入、覆盖导入、收起等 7 个处理函数；导入失败以中文弹窗说明原因 | 已验证 |
| 5 | 修复 | `pages/settings/settings.wxml` | 新增「进度备份」卡片：stat-line + 两个 textarea 区块（导出结果 / 导入输入）+ 合并 / 覆盖按钮 | 已验证 |
| 6 | 修复 | `pages/settings/settings.wxss` | 新增 `.backup-box` / `.backup-text` / `.btn-plain` | 已验证 |
| 7 | **修复已知 bug** | `utils/store.js` | `cloudStatus().enabled` 在未开通云开发时恒为 `true`，设置页谎称"打开几天后会自动创建"。改为在 `init()` / `syncNow()` 先探测 `db()` 可用性写入 `cloudAvailable`，三者联合判断 | 已验证 |
| 8 | 文档 | `README.md` | 修正过时的插件 / 云同步描述；「关于」弹窗改为备份说明 | 已验证 |
| 9 | 文档 | `AI-CONTEXT.md` | 新增：给未来接手 AI 的决策背景文档，含 3 个关键决策、已修坑列表、实测数据、TODO、主体约束 | 已验证 |

### 本次验证

```
36 条断言全部通过（mock 微信运行时，cloud = undefined）
  1. 导出格式与字段完整性                6 条
  2. 合并导入（并集、去重、保留本地释义） 8 条
  3. 覆盖导入（清理旧数据）               5 条
  4. 脏数据与恶意输入（污染/截断/非法值） 10 条
  5. 错误处理（空串/非法 JSON/其他 App）   4 条
  6. 往返一致性                            3 条
未开通云开发时 cloudStatus(): enabled=false ready=false hasDoc=false  ✅
JS 语法：12 个文件全部通过
WXML 绑定方法 15 个，JS 均有实现，无缺失
```

### 变更量

| 文件 | 变化 |
|---|---|
| `utils/store.js` | 8,973 → 约 12,400 字节（新增备份模块 + 状态修正） |
| `pages/settings/settings.js` | 3,712 → 约 5,300 字节 |
| `pages/settings/settings.wxml` | 3,191 → 约 4,600 字节 |
| `pages/settings/settings.wxss` | 877 → 1,165 字节 |

---

## 2026-09-20 · 语音引擎重写 + 18 项修复 + 个人主体适配

### 背景

从 H5 版迁移而来，原始痛点：**H5 无法选择发音人**。迁到小程序后，排查出 18 项缺陷（3 项致命、5 项重要）。同时确认一个硬约束：**个人主体小程序搜不到微信同声传译插件**（AppID `wx069ba97219f66d99`）。

### 修复清单

| # | 级别 | 文件 | 问题 | 修复方式 | 状态 |
|---|---|---|---|---|---|
| 1 | 致命 | `pages/weeks/weeks.js` | 用 `navigateTo` 跳 tabBar 页面，按官方限制必失败，周计划点任何一天都跳不过去 | 改 `switchTab` + `app.globalData.jumpDay` 传参 | 已验证 |
| 2 | 致命 | `utils/speech.js` | `state.plugin.textToSpeech` 未判空，插件不可用时直接 TypeError | 所有发声路径前判空 + toast 提示 | 已验证 |
| 3 | 致命 | `pages/today/today.js` | 监听器在每次调用时重复注册，无限堆积 | `onLoad` 只注册一次，`onUnload` 注销；`onStateChange` 改为返回取消函数 | 已验证 |
| 4 | 重要 | `utils/speech.js` | iOS 静音键打开时吞声（原 H5 "无法选择声音"的同类根因） | 播放前调用 `wx.setInnerAudioOption({ obeyMuteSwitch: false })` | 已验证 |
| 5 | 重要 | `utils/speech.js` | 快速连点叠音 + `InnerAudioContext` 实例泄漏 | `playToken` 递增令牌 + `killAudio()` 强制销毁旧实例 | 已验证 |
| 6 | 重要 | `utils/speech.js` | 插件回调未按官方规范校验 `retcode`，错误码当成功用 | 增加 `retcode === 0` 判定 + `explainPluginError()` 翻译成中文 | 已验证 |
| 7 | 重要 | `utils/store.js` | 云同步失败一次就把 `cloudEnabled` 永久置 false，网络恢复无法自愈 | 改为失败计数 `MAX_FAILS = 3` + 「立即同步」强制重试 | 已验证 |
| 8 | 重要 | `pages/weeks/weeks.js` | 周格子用「完成数量」推断完成状态，跳天打卡全错 | 新增 `store.weekCheckedMap()` 返回 7 天真实状态 | 已验证 |
| 9 | — | `utils/speech.js` | TTS 无缓存，重复朗读反复消耗额度 | 同文本 3 小时内复用，上限 300 条 | 已验证 |
| 10 | — | `utils/speech.js` | 连读中点单词不打断队列，双音覆盖 | `speak()` 先清空队列（对齐 H5 行为） | 已验证 |
| 11 | — | `utils/store.js` | `syncNow()` "推完等 1.5 秒猜结果" | 改为真实 Promise 链 | 已验证 |
| 12 | — | `utils/store.js` | 拉取与推送并发互相覆盖 | `pullInFlight` + `pendingPush` 保证先拉后推串行 | 已验证 |
| 13 | — | `utils/store.js` | 云错误只打 console | `explainError()` 直译，设置页直接显示 | 已验证 |
| 14 | — | `pages/today/today.js` | `setData` 传了双份词汇数据（传输量翻倍） | `renderDay` 剔除 `dayData.v` | 已验证 |
| 15 | — | `pages/review/review.js` | 重点词池副本缺 `starred` 字段，永远显示未收藏 | 抽样结果回填真实状态 | 已验证 |
| 16 | — | `pages/review/review.js` | 抽词可能连续抽中同一个词 | `pickItem()` 最多重试 4 次 | 已验证 |
| 17 | — | `pages/settings/*` | 无口音选择 | 新增美式/英式 picker | 已验证 |
| 18 | — | `project.config.json` | `cloudfunctionRoot` 指向不存在的目录 | 移除该字段 | 已验证 |

### 个人主体适配（online 版本）

| 改动 | 说明 |
|---|---|
| `app.json` | 移除 `plugins` 声明（个人主体未添加插件时，硬声明可能在上传环节被卡） |
| `utils/store.js` | 默认引擎 `auto` → `online` |
| `pages/settings/settings.js` | 初始引擎改为 `online` |
| `pages/settings/settings.wxml` | 插件就绪提示改为在线通道说明 |

### 变更量统计

| 文件 | 行数变化 | 字节变化 |
|---|---|---|
| `utils/speech.js` | 170 → 341（+231 / −56） | 4,873 → 10,882 |
| `utils/store.js` | 203 → 270（+124 / −57） | 5,663 → 8,973 |
| `pages/weeks/weeks.js` | 55 → 74（+35 / −16） | 1,502 → 2,345 |
| `pages/weeks/weeks.wxml` | 43 → 49（+26 / −20） | 1,833 → 2,055 |
| `pages/review/review.js` | 108 → 128（+40 / −20） | 2,962 → 3,801 |
| `pages/settings/settings.js` | 104 → 124（+30 / −10） | 3,115 → 3,712 |
| `pages/today/today.js` | +37 / −37 | 4,301 → 4,520 |
| `app.js` | +1 / −1 | 716 → 817 |
| `project.config.json` | −1 | 576 → 534 |

数据文件（`data.js` / `data-p1~p4.js`）与 H5 版逐字节一致，未改动。

### 验证结果

- 10 个 JS 文件 `node --check` 通过
- 16 条断言全部通过（10 条基础 + 6 条 TTS），含 mock 微信运行时与 mock 插件
- 全部 JSON 合法
- WXML 绑定的方法与变量一致性检查通过，无缺失
- 数据完整性：52 周 / 1,300 词 / 去重 1,125 / 四阶段各 13 周 / 无缺项

---

## 已知遗留问题

### 1. ~~云状态显示不准确~~ → **已修复**（见第二次更新第 7 条）

原问题：`store.js` 中 `enabled: cloudReady || cloudFailCount < MAX_FAILS`。未开通云开发时 `db()` 返回 `null` 直接 reject，**不经过 catch，`cloudFailCount` 恒为 0**，导致 `enabled` 恒 `true`，设置页显示「尚未建立云端记录（打开几天后会自动创建）」——实际永远不会创建。

现已改为三者联合判断（`cloudAvailable` + `cloudReady` + 失败计数）。

### 2. ~~学习进度只有本地一份~~ → **已提供备份方案**（见第二次更新第 1~6 条）

未开通云开发时进度存在 `wx.setStorageSync('ielts-365-store')`，卸载微信 / 换机 / 清缓存会丢失。

现已在设置页提供「进度备份」：导出 JSON 文本 + 合并 / 覆盖导入。**建议养成定期导出的习惯**（每月一次即可），粘贴到「文件传输助手」或备忘录保存。

### 3. 有道 TTS 是非官方公开接口

无 SLA、无配额承诺，存在被限流或改版的可能。若将来失效，替换点集中在 `speech.js` 的 `startOnline()`，约 20 行。备选：腾讯云 TTS（免费 800 万字符 / 3 个月，后付费 0.2~0.3 元每万字符），但需要在服务端加签，成本转移到云函数。

### 4. 量级参考

全库会朗读的内容共 **79,300 字符 ≈ 7.93 万字符**：

| 内容 | 条数 | 最长字符 |
|---|---|---|
| 单词 | 1,300 | 23 |
| 词汇例句 | 1,300 | 51 |
| 语法例句 | 780 | **222** |

注意：**127 条语法例句超过微信插件的 50 字符上限**，即便能用插件也读不了这部分——这是插件方案的结构性缺陷，也是本次选择在线通道的实证依据。

---

## 主体相关结论（不涉代码，但影响后续决策）

| 结论 | 依据 |
|---|---|
| 个人主体无法添加同声传译插件 | 用户已实际确认搜索不到；社区答复"个人不支持，因为这个插件的类目个人主体没有" |
| **个人主体不能迁移为企业/个体工商户主体** | 微信官方社区明确答复："个人主体小程序不支持迁移至任何主体类型……建议注册新的小程序账号" |
| 更换主体 = 重新注册，AppID 会变 | 同上 |
| 使用「经营者非本人」的个体工商户执照风险高 | 所有权归经营者，认证/变更全程需对方配合，收款进对方账户 |
| 自办个体工商户执照成本极低 | 多地政府指南：不收费、0.5~3 个工作日，部分自助机 15 分钟出照 |
| 云开发免费环境在小程序发布后第 15 天到期 | 微信官方计费文档 + 腾讯云 CloudBase 价格文档 |

**当前决策：保持个人主体，使用有道在线 TTS 通道，不开通云开发付费。**
