# AI-CONTEXT.md

> **如果你是一个刚拿到这份代码的人工智能或开发者，请先读完这一个文件。**
>
> 它记录了本项目中几个「看起来奇怪但刻意如此」的设计。这些决定都踩过坑，如果你不理解背景就"顺手优化"，很可能把修好的 bug 改回去。完整变更历史见 [CHANGELOG.md](./CHANGELOG.md)。

---

## 0. 一分钟速览

| 项目 | 状态 |
|---|---|
| 这是什么 | 雅思学习计划微信小程序，4 阶段 / 52 周 / 364 天 / 1300 词，从 H5 版迁移而来 |
| 主体类型 | **个人主体**（决定了下面很多限制） |
| 语音方案 | **有道在线 TTS**（不是微信官方插件，有原因，见 §2.1） |
| 进度存储 | **本地优先**，云开发可选且不建议依赖（见 §2.2） |
| 进度备份 | 设置页导出 / 导入 JSON 文本，支持合并与覆盖（见 §2.3） |
| 数据是否完成 | 是。52 周无缺项，已实证校验 |

**如果你要改的东西涉及「语音发声」或「进度存储」，务必先读 §2 和 §3。**

---

## 1. 为什么从 H5 迁过来

原 H5 版的核心痛点：**无法选择发音人 / 音色**。浏览器 `SpeechSynthesis` 在不同系统、不同浏览器上可用的声音完全不同，有的设备甚至没有英文语音包，用户反馈"总是无法选择声音"。

迁到小程序后用 `InnerAudioContext` + 固定 TTS 接口，输出可控。

---

## 2. 三个关键决策（**改动前必读**）

### 2.1 语音：为什么放弃微信官方同声传译插件

**原本的设计**（README 早期版本、以及仓库里 2026-09-20 之前的提交）是用微信官方插件 WechatSI：

```json
"plugins": { "WechatSI": { "version": "0.3.6", "provider": "wx069ba97219f66d99" } }
```

**改成有道在线 TTS 的三条实证依据：**

1. **个人主体根本搜不到这个插件。** 添加路径是「mp.weixin.qq.com → 设置 → 第三方设置 → 插件管理 → 添加插件」，用户实际搜索确认无结果。微信开放社区的答复是："个人不支持，因为这个插件的类目个人主体没有。"
2. **50 字符硬上限，本项目有内容读不了。** 实测全库会朗读的内容：

   | 内容 | 条数 | 最长字符 | 超 50 字符条数 |
   |---|---|---|---|
   | 单词 | 1,300 | 23 | 0 |
   | 词汇例句 | 1,300 | 51 | 1 |
   | **语法例句** | 780 | **222** | **127** |

   也就是说，即使是企业主体能用插件，**127 条语法例句（阶段四的长难句与作文范文）也永远读不出来**。
3. **成本不需要为此付费。** 全库 79,300 字符，即便放大 100 倍也只有 793 万字符。腾讯云免费额度是 800 万字符 / 3 个月，后付费 0.2~0.3 元每万字符——**语音本身的钱可以忽略**。真正的成本在于所有正规云 TTS 都需要 SecretId/SecretKey 签名，小程序前端放密钥等于泄露，必须搭云函数中转，光服务器成本就远超语音本身。

**现在的方案：**

```js
// utils/speech.js:80
return 'https://dict.youdao.com/dictvoice?audio=' + encodeURIComponent(text) + '&type=' + type;
// type=2 美式，type=1 英式
```

**使用前提（最容易漏的一步）**：必须在小程序后台「开发管理 → 开发设置 → 服务器域名」的 **downloadFile 合法域名** 中加入 `https://dict.youdao.com`。没配的话所有朗读无声，这是最常见的"坏了"的原因。

**已知风险**：有道是**非官方公开接口**，无 SLA、无配额承诺，可能被限流或改版。若将来失效，替换点集中在 `speech.js` 的 `startOnline()`，约 20 行。

> ⚠️ **请不要**因为"官方插件更正规"就改回去。理由见上面三条，尤其是第 2 条——插件方案是**结构性残缺**的。

### 2.2 云同步：为什么降级为「可选」

**原本的设计**：所有进度通过微信云开发的云数据库自动同步。

**为什么改**：按微信官方计费文档与腾讯云 CloudBase 价格文档，**免费体验环境在小程序发布后，到期时间变更为上线后的第 15 天**。之后需要转为付费环境（个人版约 19.9 元/月）才能继续。

对一个自用为主的学习小程序，为「多端同步」这一个功能付年费 200+ 元不划算。所以现在的定位是：

- **云开发 = 可选增强**。不开也能正常使用，进度存本机 `wx.setStorageSync('ielts-365-store')`。
- **降级是静默且安全的**。`store.js` 里 `saveLocal()` 在任何路径都会执行，`MAX_FAILS = 3` 控制重试频率。环境到期后不会崩溃、不会丢已有数据，只是停止同步。
- **最坏情况是数据只有一个副本**：卸载微信、换手机、长期不用被清理 → 全部丢失。所以必须配合下面 §2.3 的备份。

> ⚠️ **请不要**把云同步改回"必需"。也不要假设 `cloudStatus().enabled === true` 就意味着真的能同步（这个坑已经修过，见 §3 第 1 条）。

### 2.3 进度备份：为什么必须存在

因为 §2.2 的结论（数据只有本地一份），本项目在设置页加了**导出 / 导入**功能，不依赖任何云服务、任何主体、永不过期。

**实现要点**（`store.js` 的 `exportBackup` / `importBackup`）：

| 设计 | 原因 |
|---|---|
| 导出为 JSON **文本** | 可粘贴到「文件传输助手」、备忘录、邮件，跨平台零依赖 |
| **合并（merge）为默认推荐** | 手机和电脑各自学了一段后，合并导入即可取并集，实际起到"人工同步"作用 |
| 覆盖（replace）作为备选 | 换机恢复场景 |
| 打卡取并集，**重点词按 word 去重且保留本地已有释义** | 避免导入时把本地更完整的内容冲掉 |
| 外部数据全部逐字段校验 | 见下 |

**安全处理**（`sanitize()`）：所有外部输入都被当作不可信——

- `checkedDays` 的 key 必须匹配 `/^w\d{1,2}d[1-7]$/`，防止 `__proto__` 原型污染
- `starredWords` 必须含 `w` 字段，缺失即丢弃
- 字符串截断：单词 60 / 释义 80 / 词性 20 / 例句 300 字符
- `rate` 限 0.5~2，`accent` 限 `us|uk`，`engine` 限 `auto|plugin|online`，`startDate` 需匹配 `YYYY-MM-DD`
- 解析失败、App 标识不符、空输入均抛出**中文**错误提示

已覆盖 36 条断言，包括脏数据、恶意输入、往返一致性。

---

## 3. 已修复的坑（改回去会出事）

| # | 位置 | 坑 | 为什么这么修 |
|---|---|---|---|
| 1 | `store.js` `cloudStatus()` | `cloudFailCount` 在未开通云开发时不自增，导致 `enabled` 恒为 `true`，设置页显示"稍后会自动创建"但实际永远不会 | `init()` 与 `syncNow()` 中先探测 `db()` 是否可用，写入 `cloudAvailable`；未开通时 `enabled` 返回 `false` |
| 2 | `pages/weeks/weeks.js` | 用 `wx.navigateTo` 跳转 tabBar 页面，按官方限制**必然失败** | 必须用 `wx.switchTab`；跨页传参改用 `app.globalData.jumpDay`（switchTab 不支持 query） |
| 3 | `utils/speech.js` | `state.plugin.textToSpeech` 未判空，插件不可用时直接 TypeError | 所有发声路径前判空 + toast 提示 |
| 4 | `utils/speech.js` | iOS 静音键打开时静音——这正是原 H5 "无法选择声音"的同类根因 | 播放前调用 `wx.setInnerAudioOption({ obeyMuteSwitch: false })` |
| 5 | `utils/speech.js` | 快速连点导致叠音 + `InnerAudioContext` 实例泄漏 | `playToken` 递增令牌，旧回调作废、旧实例强制销毁 |
| 6 | `utils/speech.js` | 未按官方规范校验 `retcode`，把错误响应当成功处理 | `retcode === 0` 才算成功，其余走 `explainPluginError()` 翻译成中文 |
| 7 | `utils/store.js` | 拉取失败一次就永久禁用同步，网络恢复无法自愈 | 改为失败计数 `MAX_FAILS = 3`，「立即同步」可强制重试 |
| 8 | `pages/weeks/weeks.js` | 周格子用"完成数量"推断完成状态，跳天打卡会全错 | 新增 `weekCheckedMap()` 返回 7 天真实布尔值 |
| 9 | `pages/today/today.js` | 监听器每次调用重复注册，无限堆积 | `onLoad` 只注册一次、`onUnload` 注销；`onStateChange` 返回取消函数 |
| 10 | `pages/review/review.js` | 重点词池的副本缺 `starred` 字段，永远显示"未收藏" | 抽样结果回填真实状态 |

其余 8 项（`setData` 双份传输、抽词重复、`syncNow` 猜结果、pull/push 并发覆盖等）见 [CHANGELOG.md](./CHANGELOG.md)。

---

## 4. 可直接引用的实测数据

```
周数 52 · 词条 1300（去重 1125）· 词汇例句 1300（去重 1295）· 语法例句 780
会朗读的字符总量 79,300（≈7.93 万字符）
单词字符 9,592 | 词汇例句 38,610 | 语法例句 31,098
语法例句最长 222 字符，127 条超 50 字符（微信插件上限）
代码包约 340KB，远低于 2MB 限制，无需分包
```

数据来源：对 `utils/data.js` 全字段扫描，可复现。

---

## 5. 文件职责

```
project.config.json          appid、编译配置
miniprogram/
├── app.js                   云开发初始化（env 未填 → 走默认环境）
├── app.json                 页面注册 / tabBar / 【已移除 plugins 声明】
├── utils/
│   ├── data.js              52 周数据汇总 + 天数计算 + learnedWords()
│   ├── data-p1~p4.js        四阶段课程数据（与 H5 版逐字节一致，勿轻易改动）
│   ├── speech.js            语音引擎：发音/口音/缓存/连读队列/变更后的在线通道
│   └── store.js             进度：本地 + 云同步 + 备份导出导入
└── pages/
    ├── today/               今日计划：打卡、词汇、语法、练习
    ├── weeks/               52 周总览，点日期跳今日（用 switchTab）
    ├── review/              随机抽查（30% 优先重点词）
    └── settings/            日期 / 语速 / 引擎 / 口音 / 云同步 / 进度备份
```

---

## 6. 尚未处理的事（TODO）

1. **间隔重复复习算法未移植**。H5 版有 1/2/4/7/14/30/60/120 天的复习间隔，小程序版只有简单随机抽查。
2. **连续打卡（streak）统计**。H5 版有，小程序版未实现。
3. **播放超时保护**。若 TTS 无响应，按钮可能一直处于高亮状态，目前没有超时降级。
4. **有道接口失效兜底**。目前单点，可考虑轮询其他音源或加本地提示。

---

## 7. 主体相关的硬性约束（影响功能选型）

| 结论 | 依据 |
|---|---|
| 个人主体搜不到同声传译插件 | 用户实测 + 社区答复 |
| **个人主体不能迁移为企业 / 个体工商户** | 微信官方社区："个人主体小程序不支持迁移至任何主体类型……建议注册新的小程序账号"。**换主体 = 重新注册，AppID 会变** |
| 不要用"经营者非本人"的营业执照 | 所有权归经营者，认证与变更全程需对方配合，收款进对方账户 |
| 自办个体工商户执照成本极低 | 多地政府指南：不收费、0.5~3 个工作日，部分自助机 15 分钟出照 |
| 「教育 > 题库 / 错题解析」类目不对个体户开放 | 提审前建议在后台类目库实测；本项目实际选用「工具 → 备忘录」（⚠️ 微信当前工具类目下**没有「效率」子类目**，旧版文档写的「工具 → 效率」是错的） |
| 云开发免费环境上线第 15 天到期 | 微信官方计费文档 + CloudBase 价格文档，所有主体一视同仁 |

---

## 8. 一句话规则

> **改语音前读 §2.1，改进度存储前读 §2.2 和 §2.3，改之前先看 §3 有没有踩过那个坑。**
> 数据文件 `data-p*.js` 是 H5 版逐字节迁移来的，除非明确要改课程内容，否则别动。
